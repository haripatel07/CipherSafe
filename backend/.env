# A 32-byte (256-bit) key for AES encryption.
# You can generate one with: openssl rand -base64 32
MASTER_ENCRYPTION_KEY="your-random-32-byte-base64-string-here"

# A secret for signing JWT tokens. Can be any random string.
JWT_SECRET_KEY="your-super-secret-jwt-string-here"

# Database connection string
DATABASE_URL="host=localhost user=postgres password=yourpassword dbname=ciphersafe port=5432 sslmode=disable"